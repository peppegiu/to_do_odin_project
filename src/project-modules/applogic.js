import { projectArray, Project } from "./project";
import Todo  from "./todo";

export let selectedProject;


const deleteTodo = (msg, project) {
  if (msg == "DELETE TODO") {
    //incomplete. Practice session was over when making this code.
  }
}